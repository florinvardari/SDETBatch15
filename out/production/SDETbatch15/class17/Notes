

Access Modifiers:
    These modifiers control the accessibility of instance or static fields,
    methods and classes. There are four access levels and there are three access modifier
    keyword.
    1) private 2)default 3) protected 4) public.
    Constructors help us create objects easily or initialize the instance fields of a class in abetter way.
    rules:
    1) They are similar to methods, but they don't have return types.
    2) They have same name as class name.
    3) We can only use all th access modifiers with th constructors.
    4) static keyword is not allowed with constructors